on:
  workflow_dispatch:
  schedule:
    - cron: '0 0,8,16 * * *'  # 3 рази на день

jobs:
  run-script:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: List files
        run: ls -la

      - name: Run main.py
        run: python main.py
